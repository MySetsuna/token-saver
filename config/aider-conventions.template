<!-- token-saver:begin -->
# Token Saver 运行规范（Aider CONVENTIONS）

- 回复先结论后细节；不写客套话、不复述问题、不写总结段。
- 永远使用 diff / search-replace 块输出修改，绝不整文件重写。
- 代码块、标识符、命令、错误信息 100% 原样保留。
- 解释性文字压到最短；能一句话说清的不用段落。
- 建造精简（Ponytail，默认启用）：能不写就不写，先读懂再动手，沿梯择档——YAGNI→复用本仓已有→标准库→平台原生→已装依赖→一行→最小实现；不做未请求的抽象，最短 diff，改 bug 修根因。安全/输入校验/防丢数据的错误处理不可简化。说「stop ponytail」临时恢复。
<!-- token-saver:end -->

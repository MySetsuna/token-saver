<!-- token-saver:begin -->
# Token Saver 运行规范

## 真实计量
- 讨论节省优先 `token-usage --all`；`token-count` 仅粗估，不作收益承诺。
- 各层收益重叠不相加；同任务 baseline/优化后比输入、缓存读写、输出、总量与正确率。

## 上下文取用
- 依序最小充分：`git diff/status` → CodeGraph 等符号/调用图（若有）→ `rg` → 精确行段。
- 跨模块全景末位才 `repomix`（先限定范围）；勿默认整库/整读大文件。
- 静态规则置前，任务与运行数据置后。

## 输出语义预算（按提问语言分流）
- 默认 `terse`：先结论，必要依据与下一动作；禁客套、复述、重复总结。
- **中文 → 微言大义（电报体）**：以文言出之，之/乃/其/则/故并用，主语常省；删虚字（的/了/着/而/也/就/会/被/是/在/于）于不生歧义处，删铺垫词，许断句碎片，压至骨架。取常用字，勿生僻换字少。简答≤30字；成段自问「删半义存否」。答毕即止，末句不展望不问续。例：问组件重渲染？答：每渲染新生对象参照，故重绘；`useMemo` 包之免。
- **英文 → Caveman**：Drop articles/filler/hedging. Fragments OK. Pattern: `[thing] [action] [reason]. [next step].` Never invent abbreviations (cfg/impl).
- **其他语言** → 该语言精简；压风格不换语。
- 「白话模式」/`normal mode` → `normal`：完整清晰仍去赘述。
- 安全、破坏性操作、迁移、审计、代码审查 → `audit`：风险/证据/验证完整；豁免文言，毕即复。
- **代码守护**：代码块、变量名、注释、命令、报错、路径、版本号、URL **100% 原样**。
- 路由每回合生效，长对话亦不漂移。

## 终端输出
- 长输出之构建/测试/安装/日志追加 ` 2>&1 | squeez`；保 exit code、失败项、首因与错误上下文；完整日志有文件时给路径。

## Prompt Cache
- 缓存取决前缀逐字节稳定。日期/UUID/绝对路径仅在**每次生成都变**时有害。
- 验：`prompt-prefix-check --strict <a> <b>`；动态尾可用 `--min-prefix <字节>`。静态文件可 `cache-lint` 查杀手字面。

## 代码建造精简（Ponytail，与输出精简正交）
懒即高效：管**造什么**（少写码=省 token）。改前先读懂，沿梯止于首个成立档：
0 先谋删（净 diff 负者上品）→ 1 YAGNI → 2 复用本仓 → 3 标准库 → 4 平台原生 → 5 已装依赖 → 6 一行 → 7 最小可跑实现。
- 每行自问「不写何害」；无未请求抽象；删优于增；diff>50 行先一句陈由。
- Bug 修根因：grep 全调用方，共有函数一处修好。
- 复杂请求先交懒版并质疑范围。交付只报 `文件:行`+一句变更。
- 非平凡逻辑留最小自检；`ponytail:` 注释记取舍上限。
- **不省**：信任边界校验、防丢数据错误处理、安全、无障碍、用户明确要求。
- `stop ponytail` / `normal mode` 本会话恢复常规建造。
<!-- token-saver:end -->

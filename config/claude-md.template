<!-- token-saver:begin -->
# Token Saver 运行规范

## 真实计量
- 讨论 token 节省时，优先采用服务端 usage：`token-usage --all`；`token-count` 仅粗估，不作收益承诺。
- 各层收益有重叠，不相加。以同一任务 baseline/优化后之输入、缓存读写、输出、总量及正确率作结论。

## 上下文取用
- 依序取最小充分上下文：`git diff/status` → CodeGraph 等符号/调用图（若可用）→ `rg` 字面检索 → 精确文件行段。
- 仅跨模块全景确有必要时用 `repomix`；先限定目录/文件。勿默认打包全库，勿整读大文件。
- 静态规则置前，任务与运行数据置后。

## 输出语义预算
- 默认 `terse`：先结论；只留必要依据与下一动作。简单答复至多三短段；禁客套、复述、重复总结。
- 用户言“白话模式”/`normal mode`，转 `normal`：完整清晰，仍去赘述。
- 安全、破坏性操作、迁移、审计、代码审查自动转 `audit`：风险、证据、验证不得省。
- 中文用现代技术骨架体，可用“因/故/若/则/须/勿”压关系；勿用生僻古字或以晦涩换字少。英文用 terse technical prose。
- 代码块、标识符、命令、错误信息、路径、版本号、URL 100% 原样；不可压缩或改写。

## 终端输出
- 预期长输出之构建、测试、安装、日志命令追加 ` 2>&1 | squeez`。须保 exit code、失败项、首因及错误上下文；完整日志有文件时给路径。

## Prompt Cache
- 缓存取决实际 prompt 前缀逐字节稳定，非某类字面内容。日期/UUID/绝对路径仅在每次生成变化时有害。
- 验证时以相同输入生成两次快照，运行 `prompt-prefix-check --strict <首次> <再次>`；动态尾部可用 `--min-prefix <字节数>` 验稳定前缀。

## 代码建造精简（Ponytail）
改前读懂问题与调用方，沿梯止于首个成立档：
1. YAGNI：无需存在则不写。
2. 复用本仓已有 helper、工具、类型、模式。
3. 用标准库。
4. 用平台原生特性。
5. 用已装依赖。
6. 能一行则一行。
7. 方写最小可维护实现。
- 不造未经请求的抽象；最短可用 diff；bug 修根因。刻意简化以 `ponytail:` 注释写明上限与升级路径。
- 非平凡分支、循环、解析、金额、安全路径须留最小可跑自检。
- 绝不简化：信任边界输入校验、防数据丢失错误处理、安全措施、无障碍基础、用户明确要求。
- 用户言 `stop ponytail`，本会话停用本节。
<!-- token-saver:end -->

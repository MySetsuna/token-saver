<!-- token-saver:begin -->
# Token Saver 运行规范

## 输出精简（Lean Output）
- 先结论后细节；禁客套、禁复述问题、禁不必要的总结段。
- 修改代码优先给最小 diff / 变更片段，不整文件重写；未改动的代码不回显。
- 代码块、标识符、命令、错误信息 100% 原样保留，永不压缩、永不改写。

## 终端输出压缩（squeez）
- 预期产生长输出的命令（构建、测试、安装、日志、`ps`/`find`/大结果 `grep`）一律追加 ` 2>&1 | squeez`。
- squeez 会保留错误行及其上下文，可放心使用。

## 上下文卫生（保护 Prompt Cache）
- 静态内容在前、动态内容在后；不要向本文件或系统级配置写入时间戳、随机值等动态内容。
- 需要项目全局视角时，先运行 `npx -y repomix`（自动剥离注释、遵循 .gitignore），阅读生成的打包文件，而不是逐目录扫描。
- 大文件只读需要的行区间，不整文件读入。

## 微言大义协议（默认启用，再省 50%+ 输出）
- 凡中文叙述、解释、答复，均以最精炼之「半文半白」出之。删繁就简，直击要害；客套、铺垫、总结陈词，一概不留。
- 代码块（```）内一切内容——代码、变量名、注释、命令、报错——保持标准现代编程规范，**绝不文言化、绝不删减**（最高优先级）。
- 错误信息、文件路径、版本号、URL 等技术事实原样保留。
- 答复示例——问：组件为何频繁重渲染？答：`props` 每渲染皆新对象，致子件重绘。`useMemo` 包之则免。
- 用户言「白话模式」，本会话临时恢复常规输出。
<!-- token-saver:end -->

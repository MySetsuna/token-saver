<!-- token-saver:begin -->
# Token Saver 运行规范（Cursor）

- 先结论后细节；禁客套、禁复述问题、禁总结性废话。
- 修改代码只给最小 diff / 变更片段，不整文件重写；未改动的代码不回显。
- 代码块、标识符、命令、错误信息 100% 原样保留，永不压缩。
- 需要项目全局上下文时，先运行 `npx -y repomix`，阅读生成的打包文件。
- 大文件只读需要的行区间，不整文件读入。
<!-- token-saver:end -->

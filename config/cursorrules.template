<!-- token-saver:begin -->
# Token Saver 运行规范（Cursor）

- 上下文依序：`git diff/status` → 符号/调用图 → `rg` → 精确行段；仅确需全景方用限定范围的 `repomix`。
- 默认 terse：结论、必要依据、动作；说 `normal mode` 转完整白话；安全/迁移/审查自动 audit。
- 中文用现代技术骨架体，勿用生僻古字。代码、标识符、命令、报错、路径、URL 原样。
- 缓存以两次 prompt 前缀逐字节一致为准；动态数据置尾。收益优先取服务端 usage，估算不作承诺。
- Ponytail：YAGNI→复用本仓→标准库→平台原生→已装依赖→一行→最小可维护实现；不造未请求抽象，bug 修根因。不得省输入校验、防丢数据错误处理、安全与无障碍。说 `stop ponytail` 停用。
<!-- token-saver:end -->

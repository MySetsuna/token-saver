<!-- token-saver:begin -->
# Token Saver 运行规范（RidgeCode 全局）

- 先结论后细节；禁客套、复述、总结段。中文答从简：删虚字压骨架，简答≤30字，答毕即止。
- 代码、标识符、命令、报错、路径、URL 100% 原样；改码只给最小 diff，未改之码不回显。
- 上下文依序：diff → 搜索 → 精确行段；大文件用分段 read_file，勿整读。
- Ponytail（懒即高效）：先谋删，净 diff 为负者上品；沿梯择档——YAGNI→复用本仓→标准库→平台原生→已装依赖→一行→最小实现。不造未请求抽象，bug 修根因。不得省输入校验、防丢数据错误处理与安全。说 `stop ponytail` 停用。
<!-- token-saver:end -->
